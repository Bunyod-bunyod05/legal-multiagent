# Streamlit Cloud runs Python 3.14, so every pin here has to have a cp314 wheel.
# The floors are not cosmetic:
#   pymupdf < 1.28          - no cp314 wheel, falls back to building MuPDF
#   langchain < 0.3.25      - pulls numpy<2, and numpy 1.x has no cp314 wheel
#   langchain-community < 0.3.26 - same numpy<2 constraint
streamlit
langchain>=0.3.27,<0.4
langchain-openai>=0.2.14,<0.4
langchain-community>=0.3.27,<0.4
langchain-qdrant>=0.2.0,<0.3
langgraph>=0.2.28,<0.7
qdrant-client>=1.12.0
pymupdf>=1.28.0
tavily-python>=0.5.0
